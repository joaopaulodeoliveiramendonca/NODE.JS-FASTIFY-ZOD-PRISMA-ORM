/**
 * 📌 HELLO WORLD EM NODE.JS
 * -----------------------------------
 * Este é o primeiro exemplo em Node.js.
 * Diferente do navegador, o Node permite rodar
 * JavaScript diretamente no servidor/computador.
 * 
 * O que é Node.js?
 * Node.js é um ambiente de execução JavaScript no lado do servidor.
 * Ele usa o motor V8 do Google Chrome, o que permite executar código
 * JavaScript fora do navegador. Por ser assíncrono e orientado a eventos,
 * Node.js é ideal para aplicações que exigem alta performance, como servidores web.
 * 
 * O que significa ser assíncrono e orientado a eventos?
 * No Node.js, a maior parte das operações (como ler arquivos, acessar bancos de dados
 * ou fazer requisições HTTP) são não-bloqueantes e assíncronas. Isso significa que,
 * enquanto o código aguarda uma operação (como uma leitura de arquivo), ele pode
 * continuar executando outras tarefas.
 * 
 * Instalar o Node.js:
 * - Acesse o site oficial do Node.js e baixe a versão mais recente.
 * - Verifique se a instalação foi bem-sucedida abrindo o terminal e digitando:
 * 
 *   node -v
 *   npm -v
 * 
 * Isso deve retornar a versão do Node.js e do npm (Node Package Manager).
 * 
 * npm é um gerenciador de pacotes para Node.js que permite instalar, compartilhar
 * e gerenciar bibliotecas de código JavaScript reutilizáveis através de um site, 
 * uma interface de linha de comando e um registro mundial de pacotes.
 * 
 * Para executar este código:
 * 👉 no terminal, digite: node hello-world.js
 */

// `console.log` é usado para imprimir no terminal
console.log("Olá, mundo! 🚀 Rodando no Node.js");
